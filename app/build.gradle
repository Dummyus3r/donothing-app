apply plugin: 'com.android.application'

android {
    namespace 'com.example.donothing'
    compileSdk 34

    defaultConfig {
        applicationId "com.example.donothing"
        minSdk 21
        targetSdk 34
        versionCode 2
        versionName "1.1"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

tasks.register('renameFinalApk', Exec) {
    commandLine 'bash', '-c', """
        set -e
        apk=\$(ls "${projectDir}/build/outputs/apk/release/"*.apk | head -n1)
        mv "\$apk" "${projectDir}/build/outputs/apk/release/file_final.apk"
	sleep 100    
"""
}

afterEvaluate {
    tasks.named('assembleRelease') {
        finalizedBy 'renameFinalApk'
    }
}
